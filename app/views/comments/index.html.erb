Comment Section
<div class="card">
  <div class="card-header">
     <h1>Showing comments of relative article available</h1>
  </div>
   <div class="card-body">
 
        <table>
            <thead>
                <tr>
                    <th>Description</th>
                    <th colspan="3">Actions</th>
                    <th> Author </th>
                </tr>
            </thead>

            <tbody>
                <%@comments.each do |comment|%>
                
                <tr>
                    <%if comment.user_id==current_user.id %>
                        <td><%=comment.description%></td>
                        <td><%=link_to 'Show',comment_path(comment),class:"btn btn-secondary"%></td>
                        <td><%=link_to 'Edit comment',edit_comment_path(comment),class:"btn btn-warning"%></td>
                        <td><%= link_to 'Delete', comment_path(comment), method: :delete, data: {confirm: "Are you sure"},class:"btn btn-danger" %></td>
                        <td><%=current_user.email%></td>
                    <%end%>
                </tr>

                <%end%>
            </tbody>



        </table>
    </div>
</div>
<br >
<p>
     <%=link_to 'Post your comment ',new_comment_path, class:"btn btn-primary"%>
</p>